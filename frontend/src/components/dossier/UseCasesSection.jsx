// UseCasesSection.jsx — stub (filled in commit 4).
import SectionCard from './SectionCard';

export default function UseCasesSection({ dept }) {
  return (
    <SectionCard id="usecases" icon="🤖" title="AI Use Cases" subtitle={dept.name}>
      <div style={{ fontSize: 12, color: '#94a3b8' }}>Pending fill.</div>
    </SectionCard>
  );
}
